<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search with EJS</title>
  
</head>
<body>
  <form action="/search" method="get">
    <input type="text" name="term" placeholder="Search...">
    <button type="submit">Search</button>
  </form>
  <div>
      <% data.forEach(item => { %>
          <p><%= item.name %></p>
      <% }); %>
  </div>
  
</body>
</html>